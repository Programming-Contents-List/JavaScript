/**
 * @title 엘리먼트 객체
 * @description 엘리먼트 객체는 key와 value를 가지고 있는 객체이다. 해당 코드는 정적이고 정보만 가지고 있는 객체이다.
 * @remark object의 type을 담당한다.
 * @typedef {Object} Element
 * @property {string} key - element's key
 * @property {string} value - element's value
 */

/** @type {Element} */
const element = {
    key : "",
    value : ""
}

export default element;