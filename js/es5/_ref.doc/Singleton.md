# Singleton (싱글톤)

싱글톤은 보통 싱글톤 패턴을 떠올리지만, 과거 객체 리터럴을 기준으로 본다면, 하나의 객체를 import/export를 통해서 하나를 참조하고 있기 때문에, 마치 싱글톤 패턴 처럼 사용되어지고 있다. 그래서 이것을 단순 싱글톤이라고 지칭을 하는 경우가 있다.

## 특징

객체 리터럴을 사용할 시에는 함수가 아니기 때문에 단일 직렬로 동작을 한다. 생성과 동시에 하나의 heap의 영역을 차지하고 있다.

### Singleton Pattern(싱글톤 패턴)
