# Section2

## 목표

> 왜 변수는 접근을 할 수가 없는지, 캡슐화가 무엇인지, 프로퍼티를 유지하되 은닉성을 보장 할 수 있게 구현할 수 없는지.

객체를 생성하고 key값을 관리할 수 있는 로직을 구현한다.

0. 작업의 과정과 순서를 다이어그램으로 정리하고 각 생성한 파일을 연결시킨다.
1. 은닉성이 보장되는 객체를 생성하라.
    a. 무엇을 은식할지 정하고 이것을 JsDoc으로 제시하라.
2. 행동을 중심으로 객체를 생성하라.
    a. 어떤 행동을 중심으로 객체를 생성했는지 JsDoc으로 제시하라.
3. 무엇을 기준으로 행동을 나눌 것인지 명확하게 나누어라.
    a. JsDoc으로 기준을 나눈 근거를 제시하라.

## 구현

- [함수 객체]()

## 문제점

## 의문과 숙제
